---
import Header from '../Header.astro';


interface Props {
    lang?: "ar" | "en";
    title?: string;
    description?: string;
    children: any;
}

const {
    lang = "ar",
    title = "Quronfula",
    description = "",
    children,
} = Astro.props;
---

<!doctype html>
<html lang={lang} dir={lang === "ar" ? "rtl" : "ltr"}>
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />

        <title>{title}</title>
        <meta name="description" content={description} />

        <!-- Prevent CLS by preloading logo -->
        <link rel="preload" href="/logo.svg" as="image" />

        <!-- Optional: Global CSS -->
        <style>
            body {
                margin: 0;
                font-family: system-ui, sans-serif;
                background: #ffffff;
            }
            main {
                padding: 20px;
            }
            footer {
                padding: 25px;
                text-align: center;
                font-size: 14px;
                color: #666;
                border-top: 1px solid #eee;
            }
        </style>
    </head>

    <body>
        <!-- Header -->
        <!-- <Header lang={lang} currentPath={Astro.url.pathname} /> -->

        <!-- Page Content -->
        <main>
            
        </main>

        <!-- Footer -->
        <footer>
            © {new Date().getFullYear()} Motiur Rehmasjfajkfh
        </footer>
    </body>
</html>
