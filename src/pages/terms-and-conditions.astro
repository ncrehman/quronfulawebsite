---
import CommonTerms from "@components/common/CommonTerms.astro";
import { getLangFromPath } from "@lib/postmethodService";

const currentPath = Astro.url.pathname;
const lang = getLangFromPath(currentPath);
---

<CommonTerms {lang}/>
