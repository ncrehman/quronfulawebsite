---
import CommonQuizList from "../../../components/commonquiz/CommonQuizList.astro";
import { getLangFromPath } from "@lib/postmethodService";

const currentPath = Astro.url.pathname;
const lang = getLangFromPath(currentPath);
---

<CommonQuizList {lang}/>
